'use strict'

var isProd = (process.env.NODE_ENV === 'production')

module.exports = {
  API_ROOT: isProd
    ? 'http://gwi-beers.herokuapp.com/api/'
    : 'http://localhost:8085/api/',
  COOKIE_PREFIX: 'pro_',
  ENABLE_DEBUG_OVERLAY: !isProd
}
